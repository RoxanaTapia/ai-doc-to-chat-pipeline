# =============================================
# AI Doc-to-Chat Pipeline - Production Dependencies
# =============================================
# Tested on Python 3.12 (recommended).
# Works on older Intel Macs (Mid-2015 MacBook Pro, macOS Monterey).
# For modern Apple Silicon / macOS Ventura+ / Linux / Windows / Streamlit Cloud:
#   - Use requirements.txt instead (newer packages + better performance).

# Core framework & UI
streamlit>=1.42.0,<1.43.0                  # Modern file uploader, fragments, stable UI

# Document processing & OCR
pymupdf>=1.25.0                            # Fast PDF text + metadata extraction
pytesseract>=0.3.13                        # Tesseract OCR wrapper (requires Tesseract binary installed)

# RAG & LangChain core (stable 0.3.x series)
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0     # Chunking utilities
pydantic>=2.7.4,<3.0.0                     # LangChain 0.3.x compatibility

# Local vector store (CPU-only, safe default)
faiss-cpu>=1.8.0,<1.9.0
numpy>=1.26.0,<2.0.0                       # Compatible with faiss-cpu 1.8.x

# Local embeddings (free, offline, no API keys)
sentence-transformers>=3.0.0               # Default: all-MiniLM-L6-v2 (fast & effective)
rank-bm25>=0.2.2                           # Sparse keyword ranking for hybrid retrieval

# Hugging Face ecosystem
transformers>=4.45.0,<5.0.0

# Torch - pinned for Intel Mac / Monterey compatibility
torch>=2.2.0,<2.3.0                        # Stable wheels for older Intel Macs

# Utilities
pyyaml>=6.0.2                              # YAML config loading
python-dotenv>=1.0.1                       # .env support for API keys

# Optional LLM providers (uncomment when needed for faster inference)
# langchain-openai>=0.3.0                  # OpenAI GPT
# langchain-groq>=0.2.0                    # Groq (fast & cheap)
# langchain-anthropic>=0.3.0               # Claude models
langchain-ollama                           # Local Ollama integration (safest local start)
ollama                                     # Ollama Python client

# Development / testing extras (optional, uncomment for local dev)
# pytest>=8.0.0                            # Unit tests
# ruff>=0.6.0                              # Linting & formatting
