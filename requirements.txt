# =============================================
# AI Doc-to-Chat Pipeline - Production Dependencies
# =============================================
# Tested on Python 3.12+ (recommended for Streamlit Cloud).
# Perfect for modern hardware: Apple Silicon (M-series), Linux, Windows, and Streamlit Community Cloud.
# Uses latest stable packages as of February 2026.

# Core framework & UI
streamlit>=1.54.0,<1.55.0                  # Modern file uploader, fragments, stable UI

# Document processing & OCR
pymupdf>=1.27.1                            # Fast PDF text + metadata extraction
pytesseract>=0.3.13                        # Tesseract OCR wrapper (requires Tesseract binary installed)

# RAG & LangChain core (stable 1.x series)
langchain>=1.2.10,<2.0.0
langchain-community>=0.4.1,<1.0.0
langchain-text-splitters>=1.1.1,<2.0.0     # Chunking utilities
pydantic>=2.12.5,<3.0.0                    # LangChain compatibility

# Local vector store (CPU-only, safe default)
faiss-cpu>=1.13.2,<1.14.0
numpy>=2.4.2,<3.0.0                        # Compatible with faiss-cpu 1.13.x

# Local embeddings (free, offline, no API keys)
sentence-transformers>=5.2.3               # Default: all-MiniLM-L6-v2 (fast & effective)

# Hugging Face ecosystem
transformers>=5.2.0,<6.0.0

# Torch - powers embeddings + future local LLMs
torch>=2.10.0                              # Stable wheels; excellent performance on modern hardware

# Utilities
pyyaml>=6.0.3                              # YAML config loading
python-dotenv>=1.2.1                       # .env support for API keys

# Optional LLM providers (uncomment when needed for faster inference)
# langchain-openai>=0.3.0                  # OpenAI GPT
# langchain-groq>=0.2.0                    # Groq (fast & cheap)
# langchain-anthropic>=0.3.0               # Claude models
langchain-ollama                           # Local Ollama integration (Milestone 4)

# Development / testing extras (optional, uncomment for local dev)
# pytest>=8.0.0                            # Unit tests
# ruff>=0.6.0                              # Linting & formatting
